Location of the model class based on the network call response.

Using retrofit, the response are a class that represent the json returned by an endpoint.

I prefer to work with classes in my app that correspond to what i need, not what the endpoints return me.